# Kosongkan/hapus baris ini jika frontend & backend di-deploy bersamaan di
# satu project Vercel (root vercel.json sudah mengarahkan /api ke backend).
# Isi hanya jika backend di-deploy terpisah:
# REACT_APP_API_URL=https://nama-project-kamu.vercel.app/api
